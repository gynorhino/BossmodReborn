/*
namespace BossMod.Dawntrail.Foray.CriticalEngagement.CriticalEngagement.CE203QuarriedAway;

public enum OID : uint
{

}

public enum AID : uint
{

}

public enum SID : uint
{

}

public enum TetherID : uint
{

}

[SkipLocalsInit]
sealed class CE203QuarriedAwayStates : StateMachineBuilder
{
    public CE203QuarriedAwayStates(BossModule module) : base(module)
    {
        TrivialPhase();
    }
}

[ModuleInfo(BossModuleInfo.Maturity.WIP,
    StatesType = typeof(CE203QuarriedAwayStates),
    ConfigType = null, // replace null with typeof(TinyMageConfig) if applicable
    ObjectIDType = typeof(OID),
    ActionIDType = null, // replace null with typeof(AID) if applicable
    StatusIDType = null, // replace null with typeof(SID) if applicable
    TetherIDType = null, // replace null with typeof(TetherID) if applicable
    IconIDType = null, // replace null with typeof(IconID) if applicable
    //PrimaryActorOID = (uint)OID.TinyMage,
    Contributors = "",
    Expansion = BossModuleInfo.Expansion.Dawntrail,
    Category = BossModuleInfo.Category.Foray,
    GroupType = BossModuleInfo.GroupType.CFC,
    GroupID = 1093u,
    NameID = 0u,
    SortOrder = 3,
    PlanLevel = 0)]
[SkipLocalsInit]
public sealed class CE203QuarriedAway(WorldState ws, Actor primary) : BossModule(ws, primary, new(152.000f, 716.000f), new ArenaBoundsCircle(20f))
{
    protected override void DrawEnemies(int pcSlot, Actor pc)
    {
        Arena.Actor(PrimaryActor);
    }
}
*/
